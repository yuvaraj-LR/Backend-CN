// Using ES6 Method
export function add(x, y) {
  return x + y;
}

export const num = 100;

function sub(x, y) {
  return x - y;
}

// JS Common Method

// this is the another way of importing the function to other page.
// module.exports.sum = (x, y) => {
//     return x + y;
// }

// function add(x, y) {
//     return x + y;
// }

// function sub(x, y) {
//     return x - y;
// }

// module.exports = {
//     add : add,
//     sub : sub
// }
